# Get the default preferences setup
DEFAULT_PREFERENCES = {'multiprocessing': False,
                       'ram_integrate': False,
                       "flip_beam" : True, # Needed for XDS
                       }